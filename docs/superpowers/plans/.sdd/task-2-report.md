# Task 2 — fieldCatalog module (合规伴填)

## Status
**DONE_WITH_CONCERNS** — all 7 tests pass; build compiles cleanly. Two concerns below (one necessary code deviation, one pre-existing environment issue).

## Files touched
- `package.json` — added `"test": "vitest run"` to scripts; vitest added to devDependencies.
- `package-lock.json` — regenerated by `npm install -D vitest`.
- `src/app/compliance/copilot/fieldCatalog.ts` — NEW. Pure-logic module: `ExtractField` / `ParsedCandidate` types, `STEP1` + `STEP3_MA` field catalogs, `getFieldsForStep`, `buildExtractSystemPrompt`, `parseExtractResponse`.
- `src/app/compliance/copilot/fieldCatalog.test.ts` — NEW. 7 vitest cases (TDD).
- `docs/superpowers/plans/.sdd/task-2-report.md` — this report.

## `npm test` result
```
Test Files  1 passed (1)
     Tests  7 passed (7)
  Duration  678ms
```
All cases green: step-1 field set; step-3 mode gating (`ma` returns m0a/m0b, `new` returns []); empty steps (2,5); system-prompt content (allowed codes + 绿地投资 definition); write-back to `setMode`; parse with low-confidence flagging (<0.8) and invalid-field dropping; bad-JSON returns [].

TDD flow followed: test first ran RED (`Cannot find module './fieldCatalog'`), then GREEN after implementation.

## Build result
`npm run build` against the configured `dist-build` outDir **fails with `EPERM` on `rmSync`/`mkdir` of `dist-build/assets`** — but this is the **pre-existing exFAT volume corruption** documented in `vite.config.ts` lines 42-45 (the workaround comment says `dist/` has a corrupt dir entry; `dist-build/` has now hit the same glitch because it was written by prior builds).

Verification that the failure is environmental, not code:
- All 72 modules transform cleanly (no TS/import errors) before the disk-write step.
- Building to a **fresh** outDir (`npx vite build --outDir dist-task2 --emptyOutDir`) **succeeds in 3.59s** with the same 72 modules and only the pre-existing >500 kB chunk-size warning. Temp dir removed after verification.
- `fieldCatalog.ts` is not in the app's build graph yet (not reachable from the entry); it cannot be the cause. The vitest run also confirms it transpiles.

Root cause is filesystem-level; fix is `chkdsk E: /f` (elevated) per the config comment, not a code change.

## Concerns
1. **Necessary import-path deviation (from verbatim).** The plan's global constraint asserts `../../components/fields` from `src/app/compliance/copilot/` resolves to `src/app/components/fields.ts` exporting `WizardApi`. That path does **not** exist. `WizardApi` actually lives at `src/app/compliance/components/fields.tsx`. Per the same constraint's instruction ("Verify these exist if an import errors"), I corrected the import in both `fieldCatalog.ts` and `fieldCatalog.test.ts` to `../components/fields` (single `../`, resolving to `src/app/compliance/components/fields`). `Mode` from `../logic/weights` was correct as written and is unchanged. esbuild strips these type-only imports so the verbatim path would have "worked" at runtime, but the corrected path is what actually typechecks and is what any downstream integration / `tsc` run needs.
2. **Build writes to `dist-build` fail on this exFAT volume** (intermittent EPERM on the already-corrupted `dist-build/assets` dir entry). This is the same class of issue already called out in `vite.config.ts` for `dist/`. Not caused by this task; code is build-clean. Repair via `chkdsk E: /f`, or point `build.outDir` at a fresh directory.
