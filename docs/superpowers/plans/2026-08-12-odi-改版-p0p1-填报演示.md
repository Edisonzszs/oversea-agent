# ODI 模块改版 · Plan 1:共享骨架(P0)+ 填报演示(P1)

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development(推荐)或 superpowers:executing-plans 逐任务实施。步骤用 `- [ ]` 复选框跟踪。
> 对应 spec:`docs/superpowers/specs/2026-08-12-odi-模块改版-design.md`。本计划只覆盖 P0+P1(填报演示纵切);P2 申报助办、P3 补全为后续独立 plan。

**Goal:** 建好 ODI 独立模块的共享骨架(统一字段池 + 字段目录 + 入口分流 + App.tsx mode 收敛),并交付可端到端跑通的**填报演示服务**(7 轮向导 + 预制场景 + 联动派生 + 伴填 + 商务委生成参考稿)。

**Architecture:** 镜像合规自查模块结构(`data/field/copilot/components/doc`)。统一字段池 `OdiField[]` 是承重墙;Guide 向导按 spec 6.2 的 7 轮逐轮填池,`odiGuideLogic` 做联动+派生(spec 6.3),`OdiCopilotPanel`(G 模式)口语抽字段(复用合规 chatPrompt 范式 + `server.js` 现有 `/api/copilot/extract`),`documentGenerator` 从字段池回填生成商务委三件。引擎纯逻辑用 vitest 单测,UI 用 /browse 验证。

**Tech Stack:** React 18 + TypeScript + Vite 6(transpile-only,**无 tsconfig**);vitest(逻辑单测);/browse(UI 交互);DeepSeek 经 `server.js` 的 `/api/copilot/*`(服务端 only);`docx` + `file-saver`(生成)。

## Global Constraints

- **纯前端**:状态存 App.tsx,无后端持久化;`server.js` 现有 `/api/copilot/extract|regulation|general-stream` 直接复用,不新增后端。
- **DeepSeek key 服务端 only**:不加 `VITE_` 前缀,不进前端包(见 `.env` / `src/server/copilot.ts`)。
- **无 tsconfig**:vite/esbuild transpile-only,不做类型检查;`npx tsc --noEmit` 不可用;结构性校验用 `npm run build`(注意 exFAT `dist/` 损坏 → 构建用 `npx vite build --outDir dist-odi` 绕过),逻辑校验用 `npm test`。
- **npm 必须用镜像**:`npm install --registry=https://registry.npmmirror.com --no-audit --no-fund`。
- **100% 内联样式**(与合规一致),无 CSS 文件 / Tailwind class。
- **确认后写入红线**:伴填抽出的字段只是候选,UI 层用户点确认才写入 `fieldPool`(同合规伴填)。
- **字段口径/联动/生成源自** `ODI流程材料/ODI填报演示与申报助办完整流程_V1.0.docx`(spec §6)。
- **非 git 仓库**:无 commit 步骤;每个任务结尾以 `npm test` 通过为检查点(checkpoint)。
- 复用资产:合规 `fieldCatalog.ts`/`chatPrompt.ts`/`parseChatResponse`/`ComplianceCopilotPanel.tsx`/`ComplianceWizard.tsx` 范式;源 POC `documentGenerator.ts`。

---

## 文件结构(本 plan 新增/改动)

```
src/app/odi/
├── data/
│   ├── types.ts                    # Task 1:OdiField/OdiProject/... 类型
│   ├── odiProjects.ts              # Task 4:mock 列表
│   └── odiScenePresets.ts          # Task 7:预制场景 + 快速体验预填
├── field/
│   ├── odiFieldCatalog.ts          # Task 2:7 轮字段目录(spec 6.2)
│   ├── odiFieldCatalog.test.ts     # Task 2
│   ├── odiGuideLogic.ts            # Task 5:联动 + 派生(spec 6.3)
│   └── odiGuideLogic.test.ts       # Task 5
├── copilot/
│   ├── odiChatPrompt.ts            # Task 8:抽字段 prompt + parse(镜像合规 chatPrompt)
│   └── odiChatPrompt.test.ts       # Task 8
├── doc/
│   ├── documentGenerator.ts        # Task 9:商务委三件(移植源 POC 子集)
│   └── documentGenerator.test.ts   # Task 9
└── components/
    ├── OdiEntry.tsx                # Task 4:分流页(填表演示 / 申报助办)
    ├── OdiSidebar.tsx              # Task 4:ODI 项目侧栏(镜像 ComplianceSidebar)
    ├── OdiGuideDetailPage.tsx      # Task 10:填报演示详情页(向导 + 伴填 + 生成)
    ├── OdiGuideWizard.tsx          # Task 6:7 步向导
    └── OdiCopilotPanel.tsx         # Task 8:伴填面板(G 模式)
改动:src/app/App.tsx(Task 3、Task 10)
```

---

## Task 1:ODI 数据模型与统一字段池类型

**Files:**
- Create: `src/app/odi/data/types.ts`
- Create: `src/app/odi/data/types.test.ts`

**Interfaces:**
- Produces: `OdiDept`, `OdiFieldStatus`, `FieldSource`, `OdiField`, `OdiService`, `OdiScene`, `OdiProjectStatus`, `GeneratedDoc`, `OdiProject`, `emptyField(code,name,round,dept)` 工厂。后续所有任务消费这些类型。

- [ ] **Step 1: 写失败测试** `src/app/odi/data/types.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { emptyField, type OdiField } from "./types";

describe("ODI types", () => {
  it("emptyField 产出 status=empty、sources=[]、updatedAt=0 的空字段", () => {
    const f: OdiField = emptyField("investment_country", "投资国家", 1, "shared");
    expect(f.code).toBe("investment_country");
    expect(f.name).toBe("投资国家");
    expect(f.value).toBe("");
    expect(f.status).toBe("empty");
    expect(f.sources).toEqual([]);
    expect(f.round).toBe(1);
    expect(f.dept).toBe("shared");
    expect(f.updatedAt).toBe(0);
  });

  it("OdiField.status 可取 confirmed/conflict/missing 等", () => {
    const f = emptyField("x", "x", 1, "commerce");
    const confirmed: OdiField["status"] = "confirmed";
    const conflict: OdiField["status"] = "conflict";
    f.status = confirmed;
    expect(f.status).toBe("confirmed");
    f.status = conflict;
    expect(f.status).toBe("conflict");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test -- src/app/odi/data/types.test.ts`
Expected: FAIL(模块未找到 / `emptyField` 未定义)

- [ ] **Step 3: 写实现** `src/app/odi/data/types.ts`

```ts
// ODI 统一字段池与项目数据模型(对应 spec §4)。

export type OdiDept = "commerce" | "ndrc" | "shared";
export type OdiFieldStatus =
  | "empty" | "recognized" | "pending_confirm" | "confirmed" | "conflict" | "missing";

export interface FieldSource {
  origin: "guide" | "upload" | "auth" | "ai" | "derived";
  material?: string;   // upload 时:来自哪份材料
  evidence?: string;   // 原文摘录 / 页码
}

export interface OdiField {
  code: string;
  name: string;
  value: string;
  sources: FieldSource[];
  status: OdiFieldStatus;
  dept: OdiDept;
  round?: 1 | 2 | 3 | 4 | 5 | 6 | 7;
  confidence?: number;  // 0-1(upload 抽取 / 伴填候选用)
  derived?: boolean;    // 派生字段不可手填
  updatedAt: number;
}

export type OdiService = "guide" | "assist";
export type OdiScene = "新设独资" | "并购" | "增资变更";
export type OdiProjectStatus =
  | "填报中" | "待校验" | "校验中" | "待处理" | "可生成" | "已完成";

export interface GeneratedDoc {
  id: string;
  name: string;
  blobUrl?: string;
  generatedAt: string;
}

export interface OdiProject {
  id: string;
  name: string;
  service: OdiService;
  scene?: OdiScene;                 // 仅 guide
  fieldPool: OdiField[];            // 统一字段池(承重墙)
  uploadedFiles: unknown[];         // P2 填,本 plan 留空数组
  validation: unknown | null;       // P2 填,本 plan 留 null
  generatedDocs: GeneratedDoc[];
  status: OdiProjectStatus;
  materialVersion?: string;
  updatedAt: string;
}

export function emptyField(
  code: string, name: string, round: 1|2|3|4|5|6|7, dept: OdiDept,
): OdiField {
  return { code, name, value: "", sources: [], status: "empty", dept, round, updatedAt: 0 };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- src/app/odi/data/types.test.ts`
Expected: PASS(2/2)

- [ ] **Step 5: 检查点**

Run: `npm test`
Expected: 全绿(原合规 17 + 新增 2)。无 commit(非 git 项目)。

---

## Task 2:ODI 字段目录(7 轮,spec §6.2)

**Files:**
- Create: `src/app/odi/field/odiFieldCatalog.ts`
- Create: `src/app/odi/field/odiFieldCatalog.test.ts`

**Interfaces:**
- Consumes: Task 1 `OdiField`, `emptyField`, `OdiDept`
- Produces: `FieldDef`(字段元数据),`ROUND_FIELDS: Record<1..7, FieldDef[]>`,`getFieldsForRound(r, scene)`(并购场景追加 R7;其余场景隐藏 R7),`allFieldCodes()`。Task 5/6/8 消费。

- [ ] **Step 1: 写失败测试** `src/app/odi/field/odiFieldCatalog.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { getFieldsForRound, allFieldCodes } from "./odiFieldCatalog";

describe("ODI 字段目录", () => {
  it("R1 含投资国家/投资方式/设立方式/注册资本/投资总额", () => {
    const codes = getFieldsForRound(1, "新设独资").map(f => f.code);
    expect(codes).toEqual(expect.arrayContaining([
      "investment_country", "investment_method", "establishment_method",
      "overseas_registered_capital", "investment_total",
    ]));
  });

  it("R4 含两个中方出资币种/金额对 + 派生字段标记", () => {
    const r4 = getFieldsForRound(4, "新设独资");
    const codes = r4.map(f => f.code);
    expect(codes).toContain("cny_currency_1"); expect(codes).toContain("cny_amount_1");
    expect(codes).toContain("cny_currency_2"); expect(codes).toContain("cny_amount_2");
    const derived = r4.find(f => f.code === "investment_total_rmb");
    expect(derived?.derived).toBe(true);
  });

  it("并购场景 R7 出现,新设独资 R7 不出现(7 轮函数返回空)", () => {
    expect(getFieldsForRound(7, "并购").length).toBeGreaterThan(0);
    expect(getFieldsForRound(7, "新设独资")).toEqual([]);
  });

  it("allFieldCodes 不重复", () => {
    const all = allFieldCodes();
    expect(new Set(all).size).toBe(all.length);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npm test -- src/app/odi/field/odiFieldCatalog.test.ts`
Expected: FAIL(模块未找到)

- [ ] **Step 3: 写实现** `src/app/odi/field/odiFieldCatalog.ts`

```ts
import type { OdiDept, OdiScene } from "../data/types";

export interface FieldDef {
  code: string; name: string; round: 1|2|3|4|5|6|7;
  dept: OdiDept; required?: boolean; derived?: boolean;
  kind?: "text" | "select" | "number" | "currency";
  options?: string[];
  note?: string;  // 口径(spec 各轮"字段口径")
}

// R1 基础信息(spec 6.2)
const R1: FieldDef[] = [
  { code: "investment_country", name: "投资国家或地区", round: 1, dept: "shared", required: true, note: "最终目的地国别;用于标准化/币种/风险提示" },
  { code: "investment_method", name: "投资方式", round: 1, dept: "shared", required: true, kind: "select", options: ["新设", "并购", "增资", "变更"], note: "决定是否触发并购专项 R7" },
  { code: "establishment_method", name: "设立方式", round: 1, dept: "commerce", required: true, kind: "select", options: ["新设独资", "合资", "并购", "增资变更"], note: "决定商务委真实性承诺书版本" },
  { code: "overseas_registered_capital", name: "境外目标企业注册资本", round: 1, dept: "shared", required: true, kind: "currency" },
  { code: "investment_total", name: "投资总额", round: 1, dept: "shared", required: true, kind: "currency", note: "金额校验基础" },
];

// R2 项目情况
const R2: FieldDef[] = [
  { code: "domestic_company_name", name: "境内公司名称", round: 2, dept: "shared", required: true, note: "企业主档复用(认证预填)" },
  { code: "overseas_company_cn", name: "境外企业中文名称", round: 2, dept: "shared", required: true },
  { code: "overseas_company_en", name: "境外企业外文名称", round: 2, dept: "shared" },
  { code: "final_destination", name: "投资目的地", round: 2, dept: "shared", required: true, note: "最终项目经营/建设/并购标的所在地" },
  { code: "business_scope", name: "经营范围", round: 2, dept: "shared", required: true },
  { code: "industry", name: "所属行业", round: 2, dept: "shared", required: true },
];

// R3 投资结构
const R3: FieldDef[] = [
  { code: "chinese_shareholder", name: "中方股东", round: 3, dept: "shared", required: true },
  { code: "chinese_ratio", name: "中方股比", round: 3, dept: "shared", required: true, kind: "number" },
  { code: "foreign_shareholder", name: "外方股东", round: 3, dept: "shared" },
  { code: "foreign_ratio", name: "外方股比", round: 3, dept: "shared", kind: "number" },
  { code: "restricted_export", name: "是否涉及限制出口产品和技术行业", round: 3, dept: "ndrc", kind: "select", options: ["是", "否"] },
  { code: "multi_country_interest", name: "是否影响一国或地区以上利益", round: 3, dept: "ndrc", kind: "select", options: ["是", "否"] },
  { code: "reg_capital_chinese_ratio", name: "注册资本中方比例", round: 3, dept: "shared", kind: "number" },
  { code: "reg_capital_foreign_ratio", name: "注册资本外方比例", round: 3, dept: "shared", kind: "number" },
];

// R4 投资金额(含派生)
const R4: FieldDef[] = [
  { code: "chinese_investment_amount", name: "中方投资额", round: 4, dept: "shared", required: true, kind: "currency" },
  { code: "foreign_investment_amount", name: "外方投资额", round: 4, dept: "shared", kind: "currency" },
  { code: "exchange_rate", name: "折算汇率", round: 4, dept: "shared", kind: "number" },
  { code: "cny_currency_1", name: "中方出资币种1", round: 4, dept: "shared" },
  { code: "cny_amount_1", name: "中方出资金额1", round: 4, dept: "shared", kind: "currency" },
  { code: "cny_currency_2", name: "中方出资币种2", round: 4, dept: "shared" },
  { code: "cny_amount_2", name: "中方出资金额2", round: 4, dept: "shared", kind: "currency" },
  // 派生
  { code: "chinese_investment_rmb", name: "中方投资额人民币", round: 4, dept: "shared", derived: true, kind: "currency" },
  { code: "foreign_investment_rmb", name: "外方投资额人民币", round: 4, dept: "shared", derived: true, kind: "currency" },
  { code: "investment_total_rmb", name: "投资总额人民币", round: 4, dept: "shared", derived: true, kind: "currency" },
];

// R5 出资安排
const R5: FieldDef[] = [
  { code: "cash_domestic", name: "现金出资_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "self_funds_domestic", name: "自有资金_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "bank_loan_domestic", name: "银行贷款_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "inkind_domestic", name: "实物出资_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "intangible_domestic", name: "无形资产出资_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "equity_domestic", name: "股权出资_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "other_domestic", name: "其他出资_境内", round: 5, dept: "shared", kind: "currency" },
  { code: "self_funds_overseas", name: "自有资金_境外", round: 5, dept: "shared", kind: "currency" },
  { code: "bank_loan_overseas", name: "银行贷款_境外", round: 5, dept: "shared", kind: "currency" },
  { code: "other_overseas", name: "其他出资_境外", round: 5, dept: "shared", kind: "currency" },
];

// R6 项目说明
const R6: FieldDef[] = [
  { code: "project_summary", name: "项目简况", round: 6, dept: "shared", required: true, note: "伴填可起草;禁编造收益/就业/税收/市场规模/经营业绩/风险事实" },
  { code: "project_significance", name: "项目意义", round: 6, dept: "shared", required: true },
];

// R7 并购专项(仅并购)
const R7: FieldDef[] = [
  { code: "merger_subsidiary_name", name: "并购实施子公司名称", round: 7, dept: "shared", required: true },
  { code: "domestic_reg_capital", name: "境内投资主体注册资本", round: 7, dept: "shared", kind: "currency" },
  { code: "merger_subsidiary_reg_capital", name: "并购实施子公司注册资本", round: 7, dept: "shared", kind: "currency" },
  { code: "merger_subsidiary_location", name: "并购实施子公司注册地点", round: 7, dept: "shared" },
  { code: "merger_background", name: "并购背景", round: 7, dept: "shared" },
  { code: "merger_equity_detail", name: "拟并购股权、资产或业务情况", round: 7, dept: "shared" },
  { code: "transaction_method", name: "交易方式", round: 7, dept: "shared" },
  { code: "funding_plan", name: "资金筹措方案", round: 7, dept: "shared" },
  { code: "preliminary_schedule", name: "初步时间安排", round: 7, dept: "shared" },
  { code: "potential_risks", name: "潜在风险及应对方案", round: 7, dept: "shared" },
  { code: "govt_service_needed", name: "需政府提供的服务", round: 7, dept: "shared" },
];

export const ROUND_FIELDS: Record<1|2|3|4|5|6|7, FieldDef[]> = {
  1: R1, 2: R2, 3: R3, 4: R4, 5: R5, 6: R6, 7: R7,
};

export function getFieldsForRound(round: 1|2|3|4|5|6|7, scene: OdiScene): FieldDef[] {
  if (round === 7 && scene !== "并购") return [];
  return ROUND_FIELDS[round];
}

export function allFieldDefs(): FieldDef[] {
  return [1,2,3,4,5,6].flatMap(r => ROUND_FIELDS[r as 1|2|3|4|5|6]).concat(R7);
}
export function allFieldCodes(): string[] {
  return allFieldDefs().map(f => f.code);
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npm test -- src/app/odi/field/odiFieldCatalog.test.ts`
Expected: PASS(4/4)

- [ ] **Step 5: 检查点**

Run: `npm test` → 全绿。

---

## Task 3:App.tsx 新增 "odi" mode + 入口路由收敛

**Files:**
- Modify: `src/app/App.tsx`(约 line 44 `AppMode` 联合;新增 odi state + handlers + 渲染分支)
- Create: `src/app/odi/components/OdiEntry.tsx`(占位分流页,Task 4 完善)

**Interfaces:**
- Consumes: Task 1 `OdiProject`
- Produces: `AppMode` 含 `"odi"`;`handleEnterOdi()` 进入 ODI 分流页;`odiProjects` state + CRUD handlers(`handleCreateOdi`/`handleSelectOdi`/...)。Task 4/10 消费。

> 说明:本任务只做 mode 收敛 + 占位入口,不动现有 odi-list/project/demo 三 mode(下线留到 P3,避免 P0 阶段破坏现网)。分流页先给两个按钮,后续任务接上。

- [ ] **Step 1: 在 App.tsx 顶部 import 区追加**

```ts
import { OdiEntry } from "./odi/components/OdiEntry";
import { MOCK_ODI_PROJECTS_NEW, type OdiProject } from "./odi/data/odiProjects";
```

(若 `MOCK_ODI_PROJECTS_NEW` 暂未建,Task 4 建;此处先 import,Task 4 补文件。)

- [ ] **Step 2: `AppMode` 联合追加 `"odi"`**

将 `type AppMode = "xiaohai" | "odi-list" | "odi-project" | "odi-demo" | "compliance";` 改为含 `"odi"`:

```ts
type AppMode = "xiaohai" | "odi-list" | "odi-project" | "odi-demo" | "compliance" | "odi";
```

- [ ] **Step 3: 新增 odi state + handlers**(在合规 state 区附近)

```ts
const [odiProjects, setOdiProjects] = useState<OdiProject[]>(MOCK_ODI_PROJECTS_NEW);
const [activeOdiId, setActiveOdiId] = useState<string | null>(null);

const handleEnterOdi = () => { setMode("odi"); setActiveOdiId(null); };
const handleSelectOdi = (id: string) => setActiveOdiId(id);
const handleCreateOdi = (p: OdiProject) => {
  setOdiProjects(prev => [p, ...prev]);
  setActiveOdiId(p.id);
};
```

- [ ] **Step 4: 在 ConversationSidebar 等入口加 "进入 ODI" 触发**(给 `ConversationSidebar` 现有 `onEnterOdiWorkbench` 旁,或在 WelcomeFrame)。最小改动:在现有 `onEnterOdiWorkbench` 调用处保留,另在 PlatformNavBar 或 WelcomeFrame 加一个 `onClick={handleEnterOdi}` 的"ODI 备案助手"入口。本步先确保 `handleEnterOdi` 能被触发(具体按钮位置 Task 4 在 OdiEntry 内自洽,这里只需 mode 可达)。

- [ ] **Step 5: 中央视图追加 odi 渲染分支**

```tsx
{mode === "odi" && (
  <OdiEntry
    projects={odiProjects}
    activeProjectId={activeOdiId}
    onEnterProject={handleSelectOdi}
    onNew={/* Task 4 接 NewOdiProjectModal */}
    onBack={() => setMode("xiaohai")}
  />
)}
```

- [ ] **Step 6: 建占位 OdiEntry** `src/app/odi/components/OdiEntry.tsx`

```tsx
export function OdiEntry(props: { onBack: () => void }) {
  return (
    <div style={{ padding: 40, display: "flex", flexDirection: "column", gap: 16 }}>
      <h2 style={{ fontSize: 20, fontWeight: 800, color: "#111827" }}>ODI 备案助手</h2>
      <div style={{ display: "flex", gap: 16 }}>
        <div style={{ flex: 1, background: "#fff", border: "1px solid #e8edf5", borderRadius: 12, padding: 20 }}>
          <b>填报演示</b><br/><span style={{ color: "#64748b", fontSize: 13 }}>7 轮引导填报 + 预制场景 + 伴填(P1 接入)</span>
        </div>
        <div style={{ flex: 1, background: "#fff", border: "1px solid #e8edf5", borderRadius: 12, padding: 20 }}>
          <b>申报助办</b><br/><span style={{ color: "#64748b", fontSize: 13 }}>上传→校验→生成(P2 接入)</span>
        </div>
      </div>
      <button onClick={props.onBack} style={{ alignSelf: "flex-start", padding: "6px 16px", borderRadius: 8, border: "1px solid #d1d5db", background: "#fff", cursor: "pointer" }}>返回</button>
    </div>
  );
}
```

- [ ] **Step 7: /browse 验证**

Run dev: `npm run dev`,/browse 打开 `http://localhost:5173/chuhai-test/`,触发"进入 ODI" → 看到分流页两个卡片 + 返回。预期:不破坏现有合规/小海页面。

- [ ] **Step 8: 检查点**:`npm test` 全绿。

---

## Task 4:ODI 项目侧栏 + 分流页 + 新建任务(填表演示)

**Files:**
- Create: `src/app/odi/data/odiProjects.ts`(mock + 工厂)
- Create: `src/app/odi/components/OdiSidebar.tsx`(镜像 `ComplianceSidebar`)
- Modify: `src/app/odi/components/OdiEntry.tsx`(接侧栏 + 新建 modal)
- Create: `src/app/odi/components/NewOdiProjectModal.tsx`(镜像 `NewComplianceProjectModal`,含重名校验)

**Interfaces:**
- Consumes: Task 1 `OdiProject`/`OdiService`/`OdiScene`;Task 3 handlers
- Produces:`createGuideProject(name, scene)`/`createAssistProject(name)` 工厂;`OdiSidebar` 组件;`NewOdiProjectModal`(选 guide/assist + scene)。Task 6/10 消费。

- [ ] **Step 1: 写 mock + 工厂** `src/app/odi/data/odiProjects.ts`

```ts
import type { OdiProject, OdiScene } from "./types";
import { allFieldDefs } from "../field/odiFieldCatalog";
import { emptyField } from "./types";

export function createGuideProject(name: string, scene: OdiScene): OdiProject {
  const pool = allFieldDefs().map(def => emptyField(def.code, def.name, def.round, def.dept));
  return {
    id: `o${Date.now()}`, name, service: "guide", scene,
    fieldPool: pool, uploadedFiles: [], validation: null, generatedDocs: [],
    status: "填报中", updatedAt: "刚刚",
  };
}
export function createAssistProject(name: string): OdiProject {
  const pool = allFieldDefs().map(def => emptyField(def.code, def.name, def.round, def.dept));
  return {
    id: `o${Date.now()}`, name, service: "assist",
    fieldPool: pool, uploadedFiles: [], validation: null, generatedDocs: [],
    status: "待上传材料", updatedAt: "刚刚",
  };
}

export const MOCK_ODI_PROJECTS_NEW: OdiProject[] = [
  createGuideProject("越南新设智能装备生产基地(示例)", "新设独资"),
];
```

- [ ] **Step 2: 写 `NewOdiProjectModal.tsx`**(镜像 `NewComplianceProjectModal.tsx`:输入名 + 重名校验 existingNames;加 service 选择 guide/assist;guide 时选 scene)。复用其内联样式与重名红框逻辑——照抄 `existingNames.some(...)` + Create 置灰。

- [ ] **Step 3: 写 `OdiSidebar.tsx`**(镜像 `ComplianceSidebar.tsx` 结构:顶部"返回小海"+ "新建"按钮、项目列表、点击进入)。Props:`projects / activeProjectId / onSelectProject / onNew / onBack`。

- [ ] **Step 4: 改 `OdiEntry.tsx`** 为"侧栏 + 中区列表"布局:

```tsx
export function OdiEntry({ projects, activeProjectId, onEnterProject, onBack }: {
  projects: OdiProject[]; activeProjectId: string | null;
  onEnterProject: (id: string) => void; onBack: () => void;
}) {
  return (
    <div style={{ display: "flex", flex: 1, overflow: "hidden" }}>
      <OdiSidebar projects={projects} activeProjectId={activeProjectId}
        onSelectProject={onEnterProject} onBack={onBack} onNew={() => { /* 开 modal */ }} />
      <div style={{ flex: 1, padding: 40 }}>
        <h2 style={{ fontSize: 20, fontWeight: 800 }}>ODI 备案助手</h2>
        <p style={{ color: "#64748b" }}>左侧选择或新建任务。填报演示 = 引导填报;申报助办 = 上传校验生成。</p>
      </div>
    </div>
  );
}
```
(实际把 modal 开关 state 提到 App.tsx 或 OdiEntry 内;镜像合规的 `showNewComplianceModal` 模式。)

- [ ] **Step 5: 接 App.tsx** — `OdiEntry` 的 `onNew` 开 `NewOdiProjectModal`,`onConfirm` 调 `handleCreateOdi(createGuideProject(...))` 或 `createAssistProject(...)`。`onEnterProject` 调 `handleSelectOdi`。

- [ ] **Step 6: /browse 验证**:新建一个 guide 任务(填表演示 + 新设独资 + 快速体验)→ 列表出现 → 点击进入(详情页 Task 10 建,此处先空白不报错)。

- [ ] **Step 7: 检查点**:`npm test` 全绿。

---

## Task 5:Guide 联动 + 派生引擎(spec §6.3)

**Files:**
- Create: `src/app/odi/field/odiGuideLogic.ts`
- Create: `src/app/odi/field/odiGuideLogic.test.ts`

**Interfaces:**
- Consumes: Task 1 `OdiField`;Task 2 field codes
- Produces:`applyLinkage(pool)`(单一中方默认)、`computeDerived(pool)`(金额人民币派生)、`shouldShowRound7(pool)`(投资方式=并购?)、`commitField(pool, code, value, origin)`(写值 + status 切换)、`getVal(pool, code)`。Task 6/10 消费。

- [ ] **Step 1: 写失败测试** `src/app/odi/field/odiGuideLogic.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { emptyField } from "../data/types";
import { applyLinkage, computeDerived, shouldShowRound7, commitField, getVal } from "./odiGuideLogic";

const pool = () => [
  emptyField("investment_method", "投资方式", 1, "shared"),
  emptyField("domestic_company_name", "境内公司", 2, "shared"),
  emptyField("chinese_shareholder", "中方股东", 3, "shared"),
  emptyField("chinese_ratio", "中方股比", 3, "shared"),
  emptyField("foreign_shareholder", "外方股东", 3, "shared"),
  emptyField("foreign_ratio", "外方股比", 3, "shared"),
  emptyField("reg_capital_chinese_ratio", "注册资中方比例", 3, "shared"),
  emptyField("chinese_investment_amount", "中方投资额", 4, "shared"),
  emptyField("foreign_investment_amount", "外方投资额", 4, "shared"),
  emptyField("exchange_rate", "汇率", 4, "shared"),
  emptyField("investment_total_rmb", "投资总额人民币", 4, "shared"),
  emptyField("chinese_investment_rmb", "中方投资额人民币", 4, "shared"),
  emptyField("foreign_investment_rmb", "外方投资额人民币", 4, "shared"),
];

describe("ODI guide logic", () => {
  it("shouldShowRound7:投资方式=并购 → true,否则 false", () => {
    let p = pool(); expect(shouldShowRound7(p)).toBe(false);
    p = commitField(p, "investment_method", "并购", "guide");
    expect(shouldShowRound7(p)).toBe(true);
  });

  it("applyLinkage:无外方股东 → 单一中方默认(中方=境内公司100%,外方空,注册资中方=100%)", () => {
    let p = pool();
    p = commitField(p, "domestic_company_name", "上海XX公司", "guide");
    p = commitField(p, "foreign_shareholder", "", "guide"); // 无外方
    p = applyLinkage(p);
    expect(getVal(p, "chinese_shareholder")).toBe("上海XX公司");
    expect(getVal(p, "chinese_ratio")).toBe("100");
    expect(getVal(p, "foreign_ratio")).toBe("");
    expect(getVal(p, "reg_capital_chinese_ratio")).toBe("100");
  });

  it("computeDerived:中方100万美元 + 汇率7.2 → 人民币720万;投资总额人民币=中+外", () => {
    let p = pool();
    p = commitField(p, "chinese_investment_amount", "100", "guide");
    p = commitField(p, "foreign_investment_amount", "50", "guide");
    p = commitField(p, "exchange_rate", "7.2", "guide");
    p = computeDerived(p);
    expect(getVal(p, "chinese_investment_rmb")).toBe("720");
    expect(getVal(p, "foreign_investment_rmb")).toBe("360");
    expect(getVal(p, "investment_total_rmb")).toBe("1080");
  });

  it("commitField 写值后 status=confirmed,origin 记入 sources", () => {
    const p = commitField(pool(), "investment_method", "新设", "guide");
    const f = p.find(x => x.code === "investment_method")!;
    expect(f.value).toBe("新设");
    expect(f.status).toBe("confirmed");
    expect(f.sources.some(s => s.origin === "guide")).toBe(true);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npm test -- src/app/odi/field/odiGuideLogic.test.ts` → FAIL。

- [ ] **Step 3: 写实现** `src/app/odi/field/odiGuideLogic.ts`

```ts
import type { OdiField } from "../data/types";

export function getVal(pool: OdiField[], code: string): string {
  return pool.find(f => f.code === code)?.value ?? "";
}
function setField(pool: OdiField[], code: string, mut: (f: OdiField) => void): OdiField[] {
  return pool.map(f => { if (f.code === code) { const c = { ...f }; mut(c); return c; } return f; });
}

export function commitField(pool: OdiField[], code: string, value: string, origin: "guide"|"upload"|"auth"|"ai"|"derived"): OdiField[] {
  return setField(pool, code, f => {
    f.value = value;
    f.status = value ? "confirmed" : "empty";
    f.sources = [...f.sources.filter(s => s.origin !== origin), { origin, evidence: value }];
    f.updatedAt = Date.now();
  });
}

export function shouldShowRound7(pool: OdiField[]): boolean {
  return getVal(pool, "investment_method") === "并购";
}

// 单一中方投资默认(spec 6.3):无外方股东时
export function applyLinkage(pool: OdiField[]): OdiField[] {
  const foreign = getVal(pool, "foreign_shareholder");
  if (foreign && foreign.trim() !== "") return pool; // 有外方,不默认
  const domestic = getVal(pool, "domestic_company_name");
  if (!domestic) return pool;
  let p = commitField(pool, "chinese_shareholder", domestic, "derived");
  p = commitField(p, "chinese_ratio", "100", "derived");
  p = commitField(p, "reg_capital_chinese_ratio", "100", "derived");
  return p;
}

// 派生:金额人民币 = 原币 × 汇率(spec 6.3)
export function computeDerived(pool: OdiField[]): OdiField[] {
  const rate = parseFloat(getVal(pool, "exchange_rate")) || 0;
  const cn = parseFloat(getVal(pool, "chinese_investment_amount")) || 0;
  const fn = parseFloat(getVal(pool, "foreign_investment_amount")) || 0;
  const cnRmb = rate ? Math.round(cn * rate) : 0;
  const fnRmb = rate ? Math.round(fn * rate) : 0;
  let p = setField(pool, "chinese_investment_rmb", f => { f.value = String(cnRmb); f.status = cnRmb ? "confirmed" : "empty"; f.derived = true; f.sources = [{ origin: "derived" }]; });
  p = setField(p, "foreign_investment_rmb", f => { f.value = String(fnRmb); f.status = fnRmb ? "confirmed" : "empty"; f.derived = true; f.sources = [{ origin: "derived" }]; });
  p = setField(p, "investment_total_rmb", f => { f.value = String(cnRmb + fnRmb); f.status = (cnRmb+fnRmb) ? "confirmed" : "empty"; f.derived = true; f.sources = [{ origin: "derived" }]; });
  return p;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npm test -- src/app/odi/field/odiGuideLogic.test.ts` → PASS(4/4)。

- [ ] **Step 5: 检查点**:`npm test` 全绿。

---

## Task 6:OdiGuideWizard — 7 步向导(UI,/browse 验证)

**Files:**
- Create: `src/app/odi/components/OdiGuideWizard.tsx`

**Interfaces:**
- Consumes: Task 1 `OdiField`/`OdiProject`;Task 2 `getFieldsForRound`;Task 5 `commitField`/`applyLinkage`/`computeDerived`/`shouldShowRound7`;合规 `fields.tsx` 的 `RadioQ`/`CheckQ`/`QuestionBlock` 渲染范式
- Produces:`OdiGuideWizard` 组件(props:`project`、`onPoolChange`)。Task 10 消费。

> UI 任务不做单测,用 /browse 验证。镜像合规 `ComplianceWizard.tsx` 的 stepper + 顺序锁 + 字段渲染 + 联动触发。

- [ ] **Step 1: 写组件骨架**(stepper 1-6 + 条件 R7、顺序锁、当前轮字段从 `getFieldsForRound(round, scene)` 渲染)。每字段渲染照合规 `QuestionBlock` + `RadioQ`/text input(内联样式)。

- [ ] **Step 2: 写入联动** — 用户改某字段 → `onPoolChange(commitField(pool, code, value, "guide"))`;每次 pool 变更后跑 `applyLinkage` + `computeDerived`(在父组件 Task 10 的 onPoolChange 里集中跑,或在本组件 useEffect)。派生字段只读显示。

- [ ] **Step 3: 顺序锁 + R7 条件** — `shouldShowRound7(pool)` 为 false 时隐藏 R7,stepper 只到 6;为 true 时第 7 步出现。

- [ ] **Step 4: /browse 验证**:
  - 进 guide 任务 → R1 填投资方式=新设 → stepper 不出现 R7;填投资方式=并购 → R7 出现。
  - R2 填境内公司名 + R3 外方股东留空 → 中方股东/股比/注册资中方自动 100%(派生)。
  - R4 填中方 100、外方 50、汇率 7.2 → 三个人民币派生值自动算(720/360/1080)。

- [ ] **Step 5: 检查点**:`npm test` 全绿(本任务无新测试,确保不回归)。

---

## Task 7:预制场景 + 快速体验预填

**Files:**
- Create: `src/app/odi/data/odiScenePresets.ts`
- Create: `src/app/odi/data/odiScenePresets.test.ts`
- Modify: `src/app/odi/data/odiProjects.ts`(`createGuideProject` 接 `mode: "快速体验"|"自定义体验"` 参数)

**Interfaces:**
- Consumes: Task 2 field codes;Task 5 `commitField`
- Produces:`SCENE_PRESETS: Record<OdiScene, Record<fieldCode, value>>`(三场景示例值);`applyPreset(pool, scene)`。Task 4/10 消费。

- [ ] **Step 1: 写失败测试** `src/app/odi/data/odiScenePresets.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { SCENE_PRESETS, applyPreset } from "./odiScenePresets";
import { allFieldDefs } from "../field/odiFieldCatalog";
import { emptyField } from "./types";

describe("scene presets", () => {
  it("三场景都有预设值对象", () => {
    expect(SCENE_PRESETS["新设独资"]).toBeDefined();
    expect(SCENE_PRESETS["并购"]).toBeDefined();
    expect(SCENE_PRESETS["增资变更"]).toBeDefined();
  });
  it("applyPreset 把预设写入池(新设独资:越南/800万)", () => {
    const pool = allFieldDefs().map(d => emptyField(d.code, d.name, d.round, d.dept));
    const filled = applyPreset(pool, "新设独资");
    const get = (c: string) => filled.find(f => f.code === c)?.value;
    expect(get("investment_country")).toBe("越南");
    expect(get("investment_total")).toContain("800");
  });
});
```

- [ ] **Step 2: 运行确认失败** → FAIL。

- [ ] **Step 3: 写实现** `src/app/odi/data/odiScenePresets.ts`

```ts
import type { OdiField, OdiScene } from "./types";
import { commitField } from "../field/odiGuideLogic";

export const SCENE_PRESETS: Record<OdiScene, Record<string, string>> = {
  "新设独资": {
    investment_country: "越南", investment_method: "新设", establishment_method: "新设独资",
    overseas_registered_capital: "500万美元", investment_total: "800万美元",
    overseas_company_cn: "越南XX智能装备有限公司", final_destination: "越南·胡志明市",
    business_scope: "智能装备制造", industry: "制造业",
    chinese_shareholder: "上海XX公司", chinese_ratio: "100",
    chinese_investment_amount: "800", foreign_investment_amount: "0", exchange_rate: "7.2",
    cash_domestic: "300", self_funds_domestic: "500",
    project_summary: "(示例)在越南胡志明市设立智能装备生产基地,服务东南亚市场。",
    project_significance: "(示例)贴近东南亚客户、降低关税与物流成本。",
  },
  "并购": {
    investment_country: "德国", investment_method: "并购", establishment_method: "并购",
    overseas_registered_capital: "1000万欧元", investment_total: "1000万美元",
    overseas_company_cn: "德国XX工业有限公司", final_destination: "德国",
    business_scope: "工业设备制造", industry: "工业设备制造",
    chinese_shareholder: "上海XX公司", chinese_ratio: "80", foreign_shareholder: "原股东", foreign_ratio: "20",
    chinese_investment_amount: "800", foreign_investment_amount: "200", exchange_rate: "7.2",
    merger_subsidiary_name: "上海XX(德国)并购实施子公司",
    merger_background: "(示例)并购德国工业设备企业,获取技术与欧洲渠道。",
  },
  "增资变更": {
    investment_country: "新加坡", investment_method: "增资", establishment_method: "增资变更",
    overseas_registered_capital: "600万美元", investment_total: "500万美元",
    overseas_company_cn: "新加坡XX研发中心", final_destination: "新加坡",
    business_scope: "软件和信息技术服务", industry: "信息传输、软件和信息技术服务业",
    chinese_shareholder: "上海XX公司", chinese_ratio: "100",
    chinese_investment_amount: "500", foreign_investment_amount: "0", exchange_rate: "7.2",
    project_summary: "(示例)对新加坡研发中心增资,扩大研发团队。",
  },
};

export function applyPreset(pool: OdiField[], scene: OdiScene): OdiField[] {
  const preset = SCENE_PRESETS[scene];
  let p = pool;
  for (const [code, value] of Object.entries(preset)) {
    if (p.some(f => f.code === code)) p = commitField(p, code, value, "guide");
  }
  return p;
}
```

- [ ] **Step 4: `createGuideProject` 接 mode 参数** — 快速体验时 `applyPreset(pool, scene)`,自定义体验留空。

- [ ] **Step 5: 运行确认通过** + 检查点:`npm test` 全绿。

---

## Task 8:OdiCopilotPanel(G 模式抽字段)+ prompt/parse

**Files:**
- Create: `src/app/odi/copilot/odiChatPrompt.ts`
- Create: `src/app/odi/copilot/odiChatPrompt.test.ts`
- Create: `src/app/odi/components/OdiCopilotPanel.tsx`(G 模式;镜像 `ComplianceCopilotPanel.tsx`)

**Interfaces:**
- Consumes: Task 2 `getFieldsForRound`;合规 `copilot/api.ts` 的 `extractFields` 调用(`/api/copilot/extract`);Task 5 `commitField`
- Produces:`buildGuidePrompt(round, scene)`、`parseGuideResponse(content, fields)`(镜像合规 `chatPrompt` 的 `buildChatSystemPrompt`/`parseChatResponse`);`OdiCopilotPanel` 组件(G 模式)。Task 10 消费。

- [ ] **Step 1: 写失败测试** `src/app/odi/copilot/odiChatPrompt.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { buildGuidePrompt, parseGuideResponse } from "./odiChatPrompt";
import { getFieldsForRound } from "../field/odiFieldCatalog";

describe("ODI guide copilot prompt/parse", () => {
  it("buildGuidePrompt 含当前轮字段码 + 严格 JSON 输出要求", () => {
    const fields = getFieldsForRound(1, "新设独资");
    const p = buildGuidePrompt(1, "新设独资");
    expect(p).toContain("investment_country");
    expect(p).toContain("只输出严格 JSON");
  });
  it("parseGuideResponse 解析 candidates,只认当前轮字段码,低置信标 lowConf", () => {
    const fields = getFieldsForRound(1, "新设独资");
    const raw = JSON.stringify({
      answer: "已为您识别投资国家与金额",
      candidates: [
        { key: "investment_country", value: "越南", confidence: 0.9, evidence: "在越南设厂" },
        { key: "investment_total", value: "800万美元", confidence: 0.5, evidence: "800万" },
        { key: "not_a_field", value: "x", confidence: 0.9, evidence: "" }, // 非法码,丢弃
      ],
    });
    const res = parseGuideResponse(raw, fields);
    expect(res.answer).toContain("识别");
    expect(res.candidates).toHaveLength(2);
    expect(res.candidates[0].field.code).toBe("investment_country");
    expect(res.candidates[0].lowConf).toBe(false);
    expect(res.candidates[1].lowConf).toBe(true); // 0.5 < 0.8
  });
  it("非法 JSON 降级为纯 answer", () => {
    const fields = getFieldsForRound(1, "新设独资");
    const res = parseGuideResponse("这不是JSON,只是回答", fields);
    expect(res.candidates).toEqual([]);
    expect(res.answer).toContain("回答");
  });
});
```

- [ ] **Step 2: 运行确认失败** → FAIL。

- [ ] **Step 3: 写实现** `src/app/odi/copilot/odiChatPrompt.ts`(镜像合规 `chatPrompt.ts` 结构)

```ts
import { getFieldsForRound, type FieldDef } from "../field/odiFieldCatalog";
import type { OdiScene } from "../data/types";

export interface GuideCandidate { field: FieldDef; value: string; confidence: number; evidence: string; lowConf: boolean; }
export interface GuideResponse { answer?: string; candidates: GuideCandidate[]; }

export function buildGuidePrompt(round: 1|2|3|4|5|6|7, scene: OdiScene): string {
  const fields = getFieldsForRound(round, scene);
  const fieldLines = fields.map((f, i) =>
    `${i + 1}. ${f.code}(${f.name})${f.options ? ";允许值:" + f.options.join("/") : ""}${f.note ? ";口径:" + f.note : ""}`
  ).join("\n");
  return [
    "你是「小海·ODI 伴填」(填报演示模式)。用户会用口语描述投资安排,你从下方【当前轮可抽取字段】里客观抽取,给 value/置信度/依据(用户原文摘录)。",
    "红线:只抽能客观判定的;不确定的整字段省略,严禁臆测;value 用允许值之一或文本。",
    "【当前轮可抽取字段】:",
    fieldLines || "(本轮无可抽取字段)",
    '只输出严格 JSON:{"answer":"通俗回应,没有则 null","candidates":[{"key":"字段码","value":"","confidence":0.0,"evidence":""}]}。无则 answer 为 null、candidates 为空数组。',
  ].join("\n");
}

export function parseGuideResponse(content: string, fields: FieldDef[]): GuideResponse {
  let obj: any;
  try { obj = JSON.parse(content); } catch { return { answer: content, candidates: [] }; }
  const answer = obj?.answer ? String(obj.answer).trim() || undefined : undefined;
  const byCode = new Map(fields.map(f => [f.code, f]));
  const candidates: GuideCandidate[] = [];
  for (const c of Array.isArray(obj?.candidates) ? obj.candidates : []) {
    const f = byCode.get(String(c?.key));
    if (!f) continue;                       // 只认当前轮字段码
    const value = c?.value == null ? "" : String(c.value);
    if (!value) continue;
    const conf = typeof c.confidence === "number" ? c.confidence : 0.5;
    candidates.push({ field: f, value, confidence: conf, evidence: String(c.evidence ?? ""), lowConf: conf < 0.8 });
  }
  return { answer, candidates };
}
```

- [ ] **Step 4: 运行确认通过** → PASS(3/3)。

- [ ] **Step 5: 写 `OdiCopilotPanel.tsx`(G 模式)** — 镜像 `ComplianceCopilotPanel.tsx`:对话区 + 输入 + 调 `/api/copilot/extract`(body `{systemPrompt: buildGuidePrompt(round,scene), userText}`)→ `parseGuideResponse(content, getFieldsForRound(round,scene))` → 候选卡(置信度+依据)→ **确认按钮**才 `onPoolChange(commitField(...))`(红线)。欢迎语用精简版("我是小海·ODI 伴填。说说您的投资安排,我帮您抽取本轮可填字段(标注置信度与依据,确认后才写入)")。

- [ ] **Step 6: /browse 验证**:R1 伴填输入"越南设独资厂,800万美元,自有500+贷款300" → 候选卡出现(投资国家=越南 / 投资总额=800万 / 投资方式=新设 …)→ 点确认 → 向导 R1 字段被填上。

- [ ] **Step 7: 检查点**:`npm test` 全绿。

---

## Task 9:商务委生成参考稿(移植源 POC documentGenerator 子集)

**Files:**
- Create: `src/app/odi/doc/documentGenerator.ts`
- Create: `src/app/odi/doc/documentGenerator.test.ts`
- 安装依赖:`docx` `file-saver` + `@types/file-saver`(`npm install docx file-saver @types/file-saver --registry=https://registry.npmmirror.com --no-audit --no-fund`)

**Interfaces:**
- Consumes: Task 1 `OdiField`;Task 5 `getVal`
- 参考:源 POC `E:\claude\出海智能体原生POC\出海智能体原型设计\出海智能体原型设计\src\app\services\documentGenerator.ts`(备案表/承诺书/可研 的 docx 构造,移植时 `DocGenContext` 改为从 `OdiField[]` 取值)
- Produces:`genOdiFormBlob(pool)`、`genCommitmentLetterBlob(pool)`、`genFeasibilityReportBlob(pool)`(返回 Blob;下载由 UI 用 `file-saver`)。Task 10 消费。

- [ ] **Step 1: 安装依赖**(见上,镜像 npmmirror)。

- [ ] **Step 2: 写失败测试** `src/app/odi/doc/documentGenerator.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { genOdiFormBlob, genCommitmentLetterBlob, genFeasibilityReportBlob } from "./documentGenerator";
import { allFieldDefs } from "../field/odiFieldCatalog";
import { emptyField } from "../data/types";
import { commitField } from "../field/odiGuideLogic";

const pool = () => {
  let p = allFieldDefs().map(d => emptyField(d.code, d.name, d.round, d.dept));
  p = commitField(p, "domestic_company_name", "上海XX公司", "guide");
  p = commitField(p, "investment_country", "越南", "guide");
  p = commitField(p, "investment_total", "800万美元", "guide");
  p = commitField(p, "establishment_method", "新设独资", "guide");
  return p;
};

describe("ODI documentGenerator", () => {
  it("genOdiFormBlob 产出非空 docx Blob", async () => {
    const blob = await genOdiFormBlob(pool());
    expect(blob).toBeInstanceOf(Blob);
    expect(blob.size).toBeGreaterThan(0);
  });
  it("genCommitmentLetterBlob 产出非空 Blob", async () => {
    const blob = await genCommitmentLetterBlob(pool());
    expect(blob.size).toBeGreaterThan(0);
  });
  it("genFeasibilityReportBlob 产出非空 Blob", async () => {
    const blob = await genFeasibilityReportBlob(pool());
    expect(blob.size).toBeGreaterThan(0);
  });
});
```

> 注:vitest 默认 jsdom 环境有 `Blob`;若 `Packer.toBlob` 在 node 环境行为不同,改用 `Packer.toBuffer` 返回 `Buffer` 并测试 `byteLength > 0`。实现时以实际环境为准(合规侧 `ComplianceCopilotPanel.smoke.test.tsx` 已验证 jsdom 可用)。

- [ ] **Step 3: 运行确认失败** → FAIL。

- [ ] **Step 4: 写实现** `src/app/odi/doc/documentGenerator.ts` — 从源 POC `documentGenerator.ts` 移植 `generateOdiFormBlob` / `generateCommitmentLetterBlob` / `generateFeasibilityReportBlob` 三个函数;把入参 `DocGenContext` 改为 `pool: OdiField[]`,用 `getVal(pool, "domestic_company_name")` 等取值(参考源 POC 的 `generateOdiFormFromFieldPoolBlob` —— 它本就是从字段池回填的版本,直接移植其 `gv()` 模式)。承诺书版本按 `getVal(pool,"establishment_method")` 分支。

- [ ] **Step 5: 运行确认通过** → PASS(3/3)。

- [ ] **Step 6: 检查点**:`npm test` 全绿。

---

## Task 10:填报演示详情页 + 端到端串接

**Files:**
- Create: `src/app/odi/components/OdiGuideDetailPage.tsx`
- Modify: `src/app/App.tsx`(odi 分支:guide 项目 → `OdiGuideDetailPage`)
- Modify: `src/app/odi/components/OdiEntry.tsx`(点击 guide 项目进入详情页)

**Interfaces:**
- Consumes: Task 1-9 全部
- Produces:`OdiGuideDetailPage`(三栏:OdiSidebar | OdiGuideWizard + 生成区 | OdiCopilotPanel),guide 端到端可用。

> UI 串接任务,/browse 端到端验证。

- [ ] **Step 1: 写 `OdiGuideDetailPage.tsx`** — 三栏布局(镜像合规 `ComplianceDetailPage.tsx` 的三栏):
  - 左:`OdiSidebar`
  - 中:`OdiGuideWizard`(向导 tab)+ 底部"生成参考稿"区(三个按钮:备案表/承诺书/可研,点 → `genXxxBlob(pool)` → `saveAs`)
  - 右:`OdiCopilotPanel`(G 模式,当前轮透传)
  - onPoolChange 集中跑 `applyLinkage` + `computeDerived`(联动/派生每次写入后触发)

- [ ] **Step 2: App.tsx odi 分支** — `activeOdiId` 命中 guide 项目 → 渲染 `OdiGuideDetailPage`(key=project.id);assist 项目暂留占位(P2)。

- [ ] **Step 3: /browse 端到端验证**(P1 验收):
  1. ODI 入口 → 新建 guide 任务(新设独资 + 快速体验)→ 进入详情页 → R1-R6 字段已预填(场景预设)。
  2. 改 R1 投资方式=新设 → 无 R7;改=并购 → R7 出现。
  3. R2 改境内公司名、R3 外方留空 → 中方股比/注册资中方自动 100%。
  4. R4 改金额/汇率 → 人民币派生自动更新。
  5. 伴填在某轮输入口语 → 候选卡 → 确认 → 字段写入。
  6. 点"生成备案表" → 浏览器下载 `境外投资备案表_上海XX公司.docx`,打开内容含越南/800万。
  7. 关代理/断网 → 向导仍可纯手填并生成(规则引擎/生成不依赖模型)。

- [ ] **Step 4: 检查点**:`npm test` 全绿;`npx vite build --outDir dist-odi` 构建通过(结构性校验)。

---

## Self-Review(已自查)

**1. Spec 覆盖(P0+P1 部分)**
- spec §3 IA(入口分流):Task 3/4 ✓
- spec §4 字段池:Task 1 ✓
- spec §4.3 字段目录:Task 2 ✓(7 轮全字段 + R7 条件)
- spec §5.1 预制场景:Task 7 ✓
- spec §5.2 七轮向导:Task 2(字段)+ Task 6(向导)✓
- spec §5.3 联动+派生:Task 5 ✓(单一中方默认 + 金额派生;企业主档复用、设立方式→承诺书版本在 Task 9 承诺书分支落实)
- spec §5.4 伴填 G:Task 8 ✓
- spec §5.5 生成参考稿(缺失留空允许):Task 9/10 ✓(`getVal` 未填→"（待补充）")
- P2(申报助办)/P3 不在本 plan —— 后续 plan 覆盖。

**2. Placeholder 扫描**:无 TBD/TODO;所有步骤含真实代码或 /browse 具体验证。Task 9 步骤 4 "移植源 POC"指明了源文件路径 + 要改的入参 + 具体函数名(`generateOdiFormFromFieldPoolBlob` 的 `gv()` 模式),非占位。

**3. 类型一致性**:`OdiField`/`OdiProject`/`FieldDef`/`GuideCandidate`/`commitField`/`getVal`/`applyLinkage`/`computeDerived`/`shouldShowRound7`/`getFieldsForRound`/`buildGuidePrompt`/`parseGuideResponse`/`genOdiFormBlob` 等在定义任务与消费任务中签名一致。

**4. 已知不在本 plan**:`设立方式→承诺书版本` 的完整分支逻辑(承诺书多版本文案)在 Task 9 只做"按 establishment_method 选版本"的最小分支,完整多版本文案若 spec 有细化后续补;`企业主档复用`(认证预填)依赖认证流程,本 plan 用 Task 5 的 `applyLinkage` 覆盖单一中方默认这条,认证预填留 P3。

---

## 执行选择

Plan 1 已存:`docs/superpowers/plans/2026-08-12-odi-改版-p0p1-填报演示.md`(非 git,未 commit)。

两种执行方式:
1. **Subagent-Driven(推荐)** — 我每个 Task 派一个新 subagent 实现,任务间做 spec+质量评审,迭代快。
2. **Inline Execution** — 本会话内按 executing-plans 批量执行,带检查点。

哪种?(P2 申报助办、P3 补全的 plan 待 Plan 1 落地后再出。)
